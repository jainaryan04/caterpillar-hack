# Serving: the API, the solver and the model it loads.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
psycopg[binary,pool]>=3.2
ortools>=9.11
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.4

# Data generation and training only. Not imported by the API -- skip these if
# you are deploying the service against the model already in models/.
sdv>=1.17

# Tests.
pytest>=8.0
httpx>=0.27          # fastapi.testclient
