torch>=2.1          # MPS works from 2.1; 2.11 is what this was run on
torchvision>=0.16
opencv-python>=4.9  # 4.13 reads .avif without extra codecs
matplotlib>=3.7     # colormaps only
numpy>=1.24
ultralytics>=8.3    # YOLO detector for the proximity alert
# --backend world additionally pulls ultralytics' CLIP fork on first run
